import SwiftUI

struct CameraPicker: View {
    var body: some View {
        RTDEmptyStateView(title: "Kamera", message: "Kamera tanaman akan dihubungkan ke perangkat.", systemImage: "camera.fill")
    }
}
