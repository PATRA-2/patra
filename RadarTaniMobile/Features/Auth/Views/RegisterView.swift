import SwiftUI

struct RegisterView: View {
    var body: some View {
        FeaturePlaceholderView(title: "Daftar Petani", message: "Form pendaftaran akan dihubungkan ke API auth.", systemImage: "person.badge.plus")
    }
}
