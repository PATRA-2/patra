import SwiftUI

struct ReportMapDetailSheet: View {
    var body: some View { FeaturePlaceholderView(title: "Detail Peta", message: "Detail pin laporan akan tampil di sheet.", systemImage: "mappin.circle.fill") }
}
