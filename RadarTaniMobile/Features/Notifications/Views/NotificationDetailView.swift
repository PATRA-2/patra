import SwiftUI

struct NotificationDetailView: View {
    var body: some View { FeaturePlaceholderView(title: "Detail Notifikasi", message: "Detail notifikasi akan tampil di sini.", systemImage: "bell.badge.fill") }
}
