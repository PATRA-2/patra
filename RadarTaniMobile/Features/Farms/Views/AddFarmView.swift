import SwiftUI

struct AddFarmView: View {
    var body: some View {
        FeaturePlaceholderView(title: "Tambah Lahan", message: "Form nama lahan, tanaman, dan lokasi akan ditambahkan di sini.", systemImage: "plus.map.fill")
    }
}
