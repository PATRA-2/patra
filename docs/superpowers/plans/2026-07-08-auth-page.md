# Auth Page Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build page 1, a Radar Tani Desa email/password login screen with mock API behavior.

**Architecture:** Keep the MVP implementation in `ContentView.swift` because the project is currently a single-screen SwiftUI app. Use view-local state for form input and an async helper for mock auth behavior.

**Tech Stack:** SwiftUI, async/await, SF Symbols, Xcode iOS app target.

## Global Constraints

- Use native SwiftUI.
- Use RTD design colors from `/Users/neuhendra/Downloads/design.md`.
- Login fields are email and password.
- Use mock API behavior, not real networking.
- Use short, direct Indonesian copy.
- No new dependencies.

---

### Task 1: Build Login Page With Mock Auth

**Files:**
- Modify: `RadarTaniMobile/ContentView.swift`

**Interfaces:**
- Consumes: SwiftUI `ContentView` app root.
- Produces: `ContentView`, `LoginView`, `MockAuthService`, `RTDColor`, and `PrimaryButtonStyle`.

- [x] **Step 1: Define behavior**

Expected behavior:
- Empty email shows `Masukkan email terlebih dahulu.`
- Empty password shows `Masukkan password terlebih dahulu.`
- Valid input shows loading text `Memeriksa akun...` then a success card.
- Email containing `error` shows `Email atau password belum sesuai. Coba lagi.`

- [x] **Step 2: Implement minimal code**

Replace default `ContentView` with a SwiftUI login page using RTD visual tokens and mock auth.

- [x] **Step 3: Verify build**

Run: `xcodebuild -project RadarTaniMobile.xcodeproj -scheme RadarTaniMobile -destination 'generic/platform=iOS Simulator' build`

Expected: `BUILD SUCCEEDED`

---

## Self-Review

- Spec coverage: login page, email/password fields, mock loading/success/error behavior, RTD styling covered.
- Placeholder scan: no placeholders remain.
- Type consistency: single-file SwiftUI types are internally consistent.
